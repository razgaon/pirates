Endpoint returning if the game has started or not.
---
parameters:
  - name: game_id
    in: path
    type: string
    required: true
    default: 1111
  - name: user
    in: path
    type: string
    required: true
    default: all
definitions:
  User:
    type: string
responses:
  200:
    description: A string if the game has started or not.
    examples:
      rgb: ['Game has started', 'Game has not started']